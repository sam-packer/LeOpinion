# LeScraper Environment Variables
# Copy this file to .env

# PostgreSQL connection URL (required)
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/lescraper

# Twitter proxy credentials (optional — only needed if proxies contain passwords)
# TWITTER_PROXIES=socks5://user:pass@host1:port,socks5://user:pass@host2:port
